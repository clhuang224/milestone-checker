import { FindingDefinition } from '../models/finding.model';

/**
 * Empty on purpose.
 *
 * The nine items that used to live here were model-generated and did not correspond to any real
 * form, so they were discarded when assessment forms arrived. Items now belong to an itemList
 * form and are authored by the therapist.
 */
export const STARTER_FINDINGS: FindingDefinition[] = [];
